{
  "name": "gravitonkv",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible KV-cache quantization tradeoff study on AWS Graviton4 CPU",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-results.mjs",
    "build:data": "npm run validate"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
